# multi-agent-llm-orchestrator
Multi-agent system using a local LLM (Ollama) to interpret queries, fetch data from external APIs, generate natural language responses, and deliver them via Telegram. Built with Python, featuring an orchestrator pattern with independent, extensible agents.
